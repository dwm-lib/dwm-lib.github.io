@page "/Json"
@inject IDataService DataService
@inject NavigationManager NavigationManager

<PageTitle>WonderEgg - Json</PageTitle>

@if (dataLoaded)
{
    <div class="container-fluid h-100">
        <code>@(json)</code>
    </div>
}
