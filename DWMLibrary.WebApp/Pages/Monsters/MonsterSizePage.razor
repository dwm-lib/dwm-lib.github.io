@page "/monster/size/{SizeName}"
@inject IDataService DataService
@inject NavigationManager NavigationManager

<PageTitle>WonderEgg - Rarity - @(SizeName)</PageTitle>

@if (dataLoaded)
{
    <div class="container">
        <div class="row">
            <h1 class="col-12">Size @(SizeName) Monsters</h1>
        </div>
        <div class="row mb-3">
            <section>
                <MonsterCardGridComponent monsters=@(monsters!.OrderBy(skill => skill.Id).ToArray()) />
            </section>
        </div>
        <div class="row mb-3">
            <div class="col-lg-6">
                <section>
                    <h2>Growths</h2>
                    <MonsterGrowthsComponent monsters=@(monsters) includeNames=true />
                </section>
            </div>
            <div class="col-lg-6">
                <section>
                    <MonsterBreedsComponent breeds=@(breeds) includeTargets=true />
                </section>
            </div>
        </div>
    </div>
}
