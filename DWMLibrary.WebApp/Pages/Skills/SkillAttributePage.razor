@page "/skill/attribute/{attrName}"
@inject IDataService DataService

<PageTitle>DWM Library - Skill Attribute - @attrName</PageTitle>

@if (dataLoaded)
{
    <div class="container">
        <div class="row mb-3">
            <h1 class="col-12">Attribute @(attrName) Skills</h1>
            <SkillCardGridComponent skills=@(skills!.OrderBy(skill => skill.Id).ToArray()) />
        </div>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <section>
                    <h2>Requirements</h2>
                    <SkillRequirementsComponent skills=@(skills) includeNames=true />
                </section>
            </div>
        </div>
    </div>
}

@if (notFound)
{
    <NotFoundComponent />
}
